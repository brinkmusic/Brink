{
  "name": "brink",
  "private": true,
  "version": "0.1.0",
  "description": "Brink repo root: tooling + git hooks (pre-commit secret guard via the prepare script). App code lives in backend/ (the FastAPI app — API + Jinja frontend, uv) and analytics/ (uv).",
  "scripts": {
    "prepare": "git config core.hooksPath .githooks || true"
  }
}
